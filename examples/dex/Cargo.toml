[package]
name = "crucible-example-dex"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
crucible = { path = "../../contracts/crucible" }
